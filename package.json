{
  "name": "backstage-versions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "copy": "tsx scripts/copy-releases.ts",
    "readmes": "tsx scripts/generate-readmes.ts",
    "resolutions": "tsx scripts/generate-resolutions.ts",
    "tables": "tsx scripts/generate-tables.ts",
    "generate": "npm run copy && npm run readmes && npm run resolutions && npm run tables",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "semver": "^7.7.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/semver": "^7.7.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
